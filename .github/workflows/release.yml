name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    # Trigger manual builds
    inputs:
      version:
        description: 'Version to release (e.g., 0.2.0)'
        required: true
        type: string

env:
  CARGO_TERM_COLOR: always
  BINARY_NAME: sentinel-modsec-agent

jobs:
  build:
    name: Build ${{ matrix.target }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            os: ubuntu-latest
            artifact: linux-x86_64
          # NOTE: aarch64-linux cross-compilation disabled - requires libmodsecurity cross-compile
          - target: aarch64-apple-darwin
            os: macos-latest
            artifact: darwin-aarch64

    steps:
      - name: Checkout agent
        uses: actions/checkout@v4
        with:
          path: agent

      - name: Checkout SDK
        uses: actions/checkout@v4
        with:
          repository: raskell-io/sentinel-agent-rust-sdk
          path: sentinel-agent-rust-sdk

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Install protoc (Linux)
        if: runner.os == 'Linux'
        run: |
          sudo apt-get update
          sudo apt-get install -y protobuf-compiler

      - name: Install protoc (macOS)
        if: runner.os == 'macOS'
        run: brew install protobuf

      - name: Install libmodsecurity (Linux)
        if: runner.os == 'Linux'
        run: |
          # Install build dependencies for modsecurity
          sudo apt-get install -y \
            libmodsecurity-dev \
            libmodsecurity3 \
            pkg-config
          # Create pkg-config file
          sudo mkdir -p /usr/lib/pkgconfig
          echo 'prefix=/usr' | sudo tee /usr/lib/pkgconfig/modsecurity.pc
          echo 'exec_prefix=${prefix}' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'libdir=${exec_prefix}/lib/x86_64-linux-gnu' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'includedir=${prefix}/include' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo '' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'Name: modsecurity' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'Description: ModSecurity library' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'Version: 3.0.13' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'Libs: -L${libdir} -lmodsecurity' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          echo 'Cflags: -I${includedir}' | sudo tee -a /usr/lib/pkgconfig/modsecurity.pc
          # Set PKG_CONFIG_PATH for subsequent steps
          echo "PKG_CONFIG_PATH=/usr/lib/pkgconfig:/usr/lib/x86_64-linux-gnu/pkgconfig" >> $GITHUB_ENV
          # Verify
          PKG_CONFIG_PATH=/usr/lib/pkgconfig pkg-config --cflags --libs modsecurity

      - name: Install libmodsecurity (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install modsecurity

      - name: Cache cargo
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            agent/target/
          key: ${{ runner.os }}-${{ matrix.target }}-cargo-${{ hashFiles('agent/**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-${{ matrix.target }}-cargo-

      - name: Build release binary
        working-directory: agent
        run: cargo build --release --target ${{ matrix.target }}

      - name: Strip binary (Linux native)
        if: matrix.target == 'x86_64-unknown-linux-gnu'
        run: strip agent/target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}

      - name: Strip binary (macOS)
        if: contains(matrix.target, 'darwin')
        run: strip agent/target/${{ matrix.target }}/release/${{ env.BINARY_NAME }}

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "version=${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
          fi

      - name: Package binary
        run: |
          mkdir -p dist
          cp agent/target/${{ matrix.target }}/release/${{ env.BINARY_NAME }} dist/
          cd dist
          tar -czvf ${{ env.BINARY_NAME }}-${{ steps.version.outputs.version }}-${{ matrix.artifact }}.tar.gz ${{ env.BINARY_NAME }}

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.artifact }}
          path: dist/*.tar.gz
          retention-days: 1

  release:
    name: Create Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Determine version
        id: version
        run: |
          if [ -n "${{ inputs.version }}" ]; then
            echo "version=${{ inputs.version }}" >> $GITHUB_OUTPUT
            echo "tag=v${{ inputs.version }}" >> $GITHUB_OUTPUT
          else
            echo "version=${GITHUB_REF#refs/tags/v}" >> $GITHUB_OUTPUT
            echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT
          fi

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List artifacts
        run: find artifacts -type f

      - name: Collect release files
        run: |
          mkdir -p release
          find artifacts -name '*.tar.gz' -exec mv {} release/ \;
          ls -la release/

      - name: Delete existing release if exists
        run: gh release delete "${{ steps.version.outputs.tag }}" --yes || true
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: v${{ steps.version.outputs.version }}
          files: release/*
          fail_on_unmatched_files: true
          generate_release_notes: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
